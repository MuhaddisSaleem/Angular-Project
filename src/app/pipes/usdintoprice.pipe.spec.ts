import { UsdintopricePipe } from './usdintoprice.pipe';

describe('UsdintopricePipe', () => {
  it('create an instance', () => {
    const pipe = new UsdintopricePipe();
    expect(pipe).toBeTruthy();
  });
});
